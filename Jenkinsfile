pipeline {
    agent any
         
         stage('files scan') {
            steps {
                script {
                     ls
                     echo "testing for reference"
                }
            }
         }
}