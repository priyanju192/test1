pipeline {
    agent any
    stages {
         
         stage('files scan') {
            steps {
                script {
                     sh 'ls'
                     echo "testing for reference"
                }
            }
         }
    }
}
