pipeline {
    agent any
    stages {
         
         stage('files scan') {
            steps {
                script {
                     ls
                     echo "testing for reference"
                }
            }
         }
    }
}
